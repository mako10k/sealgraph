# Issue #17 DIRECTION_COMPARE execution note — 2026-09-18

- Repository branch at instruction: `codex/issue-17-origin-trace-r2-wip`, HEAD `550fc11`; working tree clean.
- Governing scope: Accepted R1/R2/R3 and ADR 0032 §7, ADR 0036 §2.3–2.4, ADR 0038 §§2–4. This note does not amend their semantics or acceptance.
- P4 `SELF_COMPARE` has committed local source binding and Candidate/HEAD/exact Seal own-presence components, including local JSON/human record builders. Its accepted public-output completion condition remains open because `trace compare/v2` requires graph output.
- The user was shown that P4 was active and that the full public `trace compare/v2` output requires P5, then explicitly instructed: 「コミットしてから `DIRECTION_COMPARE` に進めて」. The requested code work proceeds within P5's accepted scope.
- `perttool task start ... DIRECTION_COMPARE` preview failed with `PTACT-108` while P4 occupies `WRITER 1`. Simulated suspension of P4 followed by P5 start failed with `PTDAG-207` because `SELF` is not reached. Neither task was falsely marked complete; PERT continues to show P4 active and P5 planned until a supported truthful lifecycle update is available.
- Execution began after the user instruction at approximately 2026-09-18 16:44 JST. The exact instant was not recorded by PERT. Code commits and this note provide the restart evidence; they do not assert a PERT start event.
- Exit evidence for P5: exact Cause graph scope and budget, independent own/upstream/downstream aggregation over shared local observations, direct and indirect paths, Candidate/HEAD separation, immutable target identity, graph conflict detection, JSON/human representation, and unchanged STALE semantics. The optional `--estimate` branch belongs to P6 and cannot be presented as implemented by P5.

## Implementation and verification readback

- Local code commit: `c912613` (`Implement bounded direction compare observation core`). It adds the bounded Cause graph observation, one shared current-source observation per key, and internal `trace-compare/v2` assembly and human rendering without estimate. The graph does not use previous-revision assertions as directional edges. An explicit historical selection lists only the selected SealID in `extra_seals`; its closure appears in `observed_seals`.
- An independent read-only review identified the original `extra_seals` error; it was corrected before the code commit. The review also noted that CLI dispatch is absent. Its concern about missing-binding recheck was checked against `observeTraceOwnSource`, which inserts an empty path entry for an unbound key; `recheckTraceOwnSources` therefore revisits that key. The public CLI path additionally rechecks the full binding digest.
- `gofmt -w .`, `go vet ./...`, `go test ./...`, `npm ci`, `npm run clone-check`, `make complexity-check`, and `make deadcode-check` passed. `clone-check` reported the existing v5/v6 structured-code duplication below its configured threshold; no new clone was reported. Tests cover Cause diamonds, revision-edge exclusion, historical selection, graph budget exhaustion, Candidate/HEAD separation, source sharing, JSON null/empty members, and local results surviving incomplete graph observation.
- The worktree was clean after the code commit. `perttool document check` passed; `dag analyze --schedule both` still reports `SELF_COMPARE=active`, `DIRECTION_COMPARE=planned`, and a remaining resource makespan of 38.667 points. No false PERT transition was recorded.
- No public `sealgraph trace compare` dispatch, help/completion, or `--estimate` result is claimed. The intended user cannot run the new comparison command yet. P4 public output, P6 estimate, later integration and user trial remain separate gates.
